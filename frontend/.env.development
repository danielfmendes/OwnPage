# Loaded automatically by `npm run dev` (Vite development mode).
# Points the frontend at the locally-running API (controller via `npm run dev:local`).
# Production builds use VITE_API_ENV from the hosting provider (Cloudflare Pages) instead.
VITE_API_ENV=http://localhost:8080
